name: Simple Test Pipeline

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "✅ Pipeline is working!"

      - name: Show files
        run: ls -la

      - name: Show Python version
        run: python --version